spring:
  datasource:
    url: jdbc:mysql://localhost:3306/om_db
    username: root
    password: S3cret
  jpa:
    hibernate:
      ddl-auto: 'update'
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL57Dialect

logging:
  level:
    com.savantdegrees: DEBUG
    org.hibernate: ERROR